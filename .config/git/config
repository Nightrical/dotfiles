[core]
    pager = delta

[include]
    path = ~/.config/delta/themes/catppuccin.gitconfig

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true  # use n and N to move between diff sections
    features = catppuccin-mocha
    side-by-side = true
[user]
	email = miracle@localhost.net
	name = Jacob
